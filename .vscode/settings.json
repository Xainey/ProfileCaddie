{
    // When enabled, will trim trailing whitespace when you save a file.
    "files.trimTrailingWhitespace": true,

    // Associate Jenkinsfile with groovy, can be done at global user scope.
    "files.associations": {
        "Jenkinsfile": "groovy"
    }
}